# {{ schema.title }}

{{ schema.description }}

{{ schema.id }}

{{#if example}}
## Example
```
{{{json example.data}}}
```
{{/if}}

## Attributes

{{attributesTable schema.properties }}

---

{{#each schema.properties}}

## {{ @key }}

{{ this.description }}

### Properties

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="2">Value</th>
    </tr>
  </thead>
  <tbody>
    <tr><td>Title</td><td colspan="2">{{ this.title }}</td></tr>
    <tr><td>Required</td><td colspan="2">{{#if this.isRequired}}Yes{{else}}No{{/if}}</td></tr>
    {{propertyTypeRow this }}
    {{#if this.enum}}<tr><td>Enum</td><td colspan="2">{{enumValues this.enum}}</td></tr>{{/if}}
    {{#if this.minLength}}<tr><td>in Length</td><td colspan="2">{{enumValues this.minLength}}</td></tr>{{/if}}
    {{#if this.maxLength}}<tr><td>Max Length</td><td colspan="2">{{enumValues this.maxLength}}</td></tr>{{/if}}
    {{#if this.format}}<tr><td>Format</td><td colspan="2">{{enumValues this.format}}</td></tr>{{/if}}
    {{#if this.pattern}}<tr><td>Pattern</td><td colspan="2">{{enumValues this.pattern}}</td></tr>{{/if}}
  </tbody>
</table>

{{/each}}